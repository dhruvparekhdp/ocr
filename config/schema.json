{
  "productName": "DocuClassify",
  "primaryColor": "#4f8cff",
  "logoUrl": null,
  "documentTypes": [
    {
      "name": "Invoice",
      "description": "Commercial invoices, tax invoices, bills — a request for payment from seller to buyer.",
      "keywords": ["Tax Invoice", "Commercial Invoice", "Invoice To", "Bill To", "Total Due", "Amount Due"],
      "keywordWeight": 5
    },
    {
      "name": "Purchase Order",
      "description": "A buyer's order authorizing a purchase, or a proforma/packing-list document that serves the same anchoring role.",
      "keywords": ["Purchase Order", "PO No", "P.O.", "Order Date", "Proforma Invoice"],
      "keywordWeight": 5
    },
    {
      "name": "Transaction Record",
      "description": "Bank/payment transaction statements, ledger entries, payment confirmations.",
      "keywords": ["Transaction ID", "Transaction Date", "Statement", "Account Number", "Debit", "Credit", "Payment Reference"],
      "keywordWeight": 5
    },
    {
      "name": "Receipt",
      "description": "Proof of payment for a completed transaction.",
      "keywords": ["Receipt No", "Receipt Number", "Paid", "Payment Received", "Amount Paid"],
      "keywordWeight": 4
    },
    {
      "name": "Unknown",
      "description": "Anything that doesn't match a configured document type.",
      "keywords": [],
      "keywordWeight": 0
    }
  ],
  "fields": [
    {
      "name": "documentNumber",
      "description": "The primary identifying number for this document — invoice number, receipt number, transaction ID, etc., whichever applies to the detected document type.",
      "labels": ["Invoice No", "Invoice Number", "Inv No", "Inv #", "Receipt No", "Receipt Number", "Transaction ID", "Transaction No", "Reference Number"],
      "appliesTo": ["Invoice", "Transaction Record", "Receipt"],
      "isBatchKey": true
    },
    {
      "name": "referenceNumber",
      "description": "A purchase-order/proforma/reference number this document points back to, or its own reference number if it is the PO/proforma document itself.",
      "labels": ["PO No", "P.O. Number", "PO Ref", "Purchase Order Number", "Reference (PXP)"],
      "appliesTo": ["Invoice", "Purchase Order"],
      "isBatchKey": true
    },
    {
      "name": "date",
      "description": "The primary date on the document — invoice date, transaction date, order date, etc.",
      "labels": ["Date", "Invoice Date", "Transaction Date", "Order Date"],
      "appliesTo": ["Invoice", "Purchase Order", "Transaction Record", "Receipt"],
      "isBatchKey": false
    },
    {
      "name": "amount",
      "description": "The total monetary amount on the document.",
      "labels": ["Total", "Total Due", "Amount Due", "Amount Paid", "Total Amount"],
      "appliesTo": ["Invoice", "Transaction Record", "Receipt"],
      "isBatchKey": false
    }
  ]
}
